DEEPSEEK_API_KEY=your_deepseek_key_here

# Fallback mode when auto-routing is inconclusive.
# Options: "local" | "cloud"
# In practice, auto-routing overrides this for most queries.
DEFAULT_MEMORY_MODE=local
